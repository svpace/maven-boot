#!/usr/bin/env bash
set -Eeuo pipefail

IFS=$'\t\n'

run-update() {
		xsltproc tools/spring.xslt plain/pom.xml \
		| unexpand -t2 > spring/pom.xml
    mvn \
        spotless:apply \
        versions:update-properties \
        versions:update-parent \
        versions:commit
}

run-${@:-help}
